{
  "name": "c",
  "version": "0.0.1",
  "description": "Quiz App implemented with React, Redux, Graphql ,Websockets and Passport",
  "author": "kalleja",
  "scripts": {
    "start": "cd server && npm install && node ./server.js",
    "app": "cd app && npm install && npm run app",
    "heroku-postbuild": "NPM_CONFIG_PRODUCTION=false cd app && npm install && npm run build"
  },
  "main": "index.js",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^5.2.0"
  }
}
